ifconfig -l

#The ifconfig utility is used to assign an
#address to a network interface and/or
#configure network interface parameters.
# Flag “-l” list all available interfaces on the system, with no other additional information. 