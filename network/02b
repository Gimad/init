arp -a
arp -a | grep 192.168.20

#The arp utility displays and modifies 
#the Internet-to-Ethernet address translation tables used by
#the address resolution protocol
#Flag “-a” displays or deletes all of the current ARP entries.
